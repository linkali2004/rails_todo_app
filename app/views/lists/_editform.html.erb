<%= form_with(model: list) do |form| %>
  <% if list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
        <% list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="d-flex  mt-3" style="gap:4px;">
  <div >
    <%= form.text_field :title , class:"form-control form-control-sm" , id:"form-textfield" %>
  </div>

  <div>
    <%= form.submit "update" , class:"btn btn-dark btn-sm" %>
  </div>
  <div>
  <%= link_to "cancel", list,class:"btn btn-dark btn-sm" %>
  </div>

</div>
<% end %>
