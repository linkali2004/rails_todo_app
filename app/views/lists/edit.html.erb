<h1>Editing list</h1>
<%= turbo_frame_tag dom_id(@list) do%>
<%= render "editform", list: @list %>

<% end %>
<br>

<div>
  <%= link_to "Show this list", @list %> |
  <%= link_to "Back to lists", lists_path %>
</div>
